import { EntityParamOptions } from "../options/EntityParamOptions";
/**
 * Creates entity from the request body.
 */
export declare function EntityFromBody(options?: EntityParamOptions): (object: Object, method: string, index: number) => void;
